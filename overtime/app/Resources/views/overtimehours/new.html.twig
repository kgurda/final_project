{% extends 'base.html.twig' %}
{% block title %}Add new overtime{% endblock %}
{% block body %}
    <h1>Overtimehour creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <div>
        <a href="{{ path('overtimehours_index') }}">
            <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span>
            Back to the list
        </a>
    </div>
{% endblock %}
