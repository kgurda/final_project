{% extends 'base.html.twig' %}
{% block title %}Welcome!{% endblock %}
{% block body %}
    <h1>Users:</h1>
        <ul>
        {% for user in users %}
            <li> {{ user.username }}
                <a href="{{ path('overtime_admin_showallbyuserid', { 'id': user.id }) }}">show</a>
            </li>
        {% endfor %}
        </ul>
    </form>
{% endblock %}